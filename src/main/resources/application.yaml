server:
  port: 8080

spring:
  application:
    name: generator-visit-result

  cloud:
    stream:
      bindings:

        input:
          destination: visits

        output:
          destination: visits-processed

        visits-in:
          destination: visits-processed
          contentType: application/json