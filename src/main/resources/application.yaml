server:
  port: 30200

spring:
  application:
    name: generator-visit-result

  cloud:
    stream:
      bindings:

        visits-before-appointmant:
          destination: visits

        visits-after-appointmant:
          destination: visits-processed

        outcomming-visits:
          destination: visits-processed
          contentType: application/json